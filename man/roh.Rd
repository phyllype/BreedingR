% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signatures.R
\name{roh}
\alias{roh}
\title{Runs of homozygosity: F_ROH per animal and per-marker frequency}
\usage{
roh(m, min_snp = 30L, max_het = 0L, pos = NULL, min_kb = NULL, chr = NULL)
}
\arguments{
\item{m}{0/1/2 matrix; NA breaks the run, it is never treated as homozygous}

\item{min_snp}{minimum run length, in consecutive markers}

\item{max_het}{heterozygotes tolerated inside a run (0 = none)}

\item{pos}{positions in base pairs, optional; with them `min_kb` also filters}

\item{min_kb}{minimum length in kb, used only when pos is given}

\item{chr}{chromosome of each marker, optional; runs do not cross chromosomes

Without a map, the run is defined by the COUNT of consecutive markers, and the limitation
is stated: panels of different density give different runs with the same min_snp.}
}
\value{
list with `animal` (F_ROH = fraction of the markers in ROH) and `marker`
  (frequency of animals in ROH at each marker: the peaks are the islands)
}
\description{
Runs of homozygosity: F_ROH per animal and per-marker frequency
}
