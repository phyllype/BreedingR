% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pedigree.R
\name{sparse_chol}
\alias{sparse_chol}
\title{Sparse Cholesky of a symmetric positive-definite matrix}
\usage{
sparse_chol(a, reorder = TRUE)
}
\arguments{
\item{a}{list i, j, x, n with the triplets of one triangle of the symmetric matrix}

\item{reorder}{FALSE factors in the natural order, so the tests can compare}
}
\value{
a list with \code{L}, the factor in triplets, \code{perm}, the permutation that was applied,
\code{logdet}, the log-determinant, and \code{dense_block}, the size of the final dense block.
}
\description{
Orders by minimum degree, permutes, and factors. Returns L in triplets, the permutation,
the log-determinant and the size of the final dense block.
}
\details{
The ordering is minimum degree and not reverse Cuthill-McKee: RCM halves the fill-in but
puts the high-degree nodes FIRST, which is exactly where the closed form of the final
dense block cannot see them.
}
