## What this changes

## The test that fails without it

New behaviour arrives with a test under `tests/testthat/`. The bar is that the test
fails without this change; one that would pass either way does not gate anything.

## Before merging

- [ ] `R CMD check` passes, locally or on the CI run for this branch
- [ ] I read `CONTRIBUTING.md` and I grant the right described under "Copyright on what
      you contribute"
